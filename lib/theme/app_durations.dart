class AppDurations {
  static const fast = Duration(milliseconds: 150);
  static const timeToRefresh = Duration(seconds: 5  );
  static const normal = Duration(milliseconds: 250);
  static const slow = Duration(milliseconds: 300);
  static const sSlow = Duration(milliseconds: 400);
  static const verySlow = Duration(milliseconds: 600);
  static const carouselAutoPlay = Duration(seconds: 5);
  static const showOverlay = Duration(seconds: 5);
}
